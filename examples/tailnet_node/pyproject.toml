[project]
name = "tailnet-node-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.265.0",
    "constructs>=10.8.1",
    "brobot-infra",
]

[tool.uv.sources]
brobot-infra = { path = "../..", editable = true }

[dependency-groups]
dev = [
    "pyrefly>=1.2.0",
    "ruff>=0.16.3",
]
